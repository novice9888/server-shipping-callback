{
  "name": "paypal-webhook-netlify",
  "version": "1.0.0",
  "private": true,
  "description": "Sample PayPal webhook + shipping callback listeners — deployable as Netlify Functions or as a standalone Express server (Render, etc).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "buffer-crc32": "^1.0.0",
    "express": "^4.19.2"
  }
}
